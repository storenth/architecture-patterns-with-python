# Архитектурные паттерны на Python: краткая сводка для будущего Team Lead

## 📌 Введение: почему это важно для руководителя разработки

Как будущий руководитель отдела разработки, вам нужно не просто писать код, а **строить системы, которые масштабируются, легко меняются и не превращаются в "большой комок грязи"** (Big Ball of Mud). Книга "Architecture Patterns with Python" (Cosmic Python) показывает, как применять проверенные архитектурные паттерны для решения реальных бизнес-проблем.

---

## 🔷 Часть I: Фундамент — Доменная модель и её окружение

### 1. Доменное моделирование (DDD)

**Для разработчика:**
Доменная модель — это не таблицы БД, а объекты, отражающие бизнес-логику. Ключевые концепции:

```python
# Entity (сущность) — имеет идентичность, которая сохраняется со временем
class Batch:
    def __init__(self, reference: str, sku: str, qty: int):
        self.reference = reference  # Идентификатор остаётся неизменным
        self.sku = sku
        self._purchased_quantity = qty

# Value Object (объект-значение) — определяется только своими атрибутами
@dataclass(frozen=True)
class OrderLine:
    orderid: str
    sku: str
    qty: int  # Изменение любого поля = новый объект

# Доменный сервис — операция, не принадлежащая конкретной сущности
def allocate(line: OrderLine, batches: List[Batch]) -> str:
    batch = next(b for b in sorted(batches) if b.can_allocate(line))
    batch.allocate(line)
    return batch.reference
```

**Для школьника:**
Представь магазин игрушек. У каждой коробки с игрушками есть **ярлык** (это `reference` — идентичность коробки). Если ты поменяешь игрушки внутри коробки — это всё та же коробка (сущность). А сама игрушка без коробки — это просто набор характеристик: цвет, размер, материал (объект-значение). Если поменять хоть одну характеристику — это уже другая игрушка.

---

### 2. Паттерн Репозиторий (Repository)

**Для разработчика:**
Абстракция над постоянным хранилищем. Позволяет работать с коллекцией объектов как с памятью:

```python
class AbstractRepository(abc.ABC):
    @abc.abstractmethod
    def add(self, batch: Batch): ...
    @abc.abstractmethod
    def get(self, reference: str) -> Batch: ...

# Реализация для тестов
class FakeRepository(AbstractRepository):
    def __init__(self, batches):
        self._batches = set(batches)
    
    def get(self, reference):
        return next(b for b in self._batches if b.reference == reference)

# В продакшене — интеграция с БД через инверсию зависимостей
class SqlAlchemyRepository(AbstractRepository):
    def __init__(self, session):
        self.session = session
    
    def get(self, reference):
        return self.session.query(Batch).filter_by(reference=reference).one()
```

**Для школьника:**
Репозиторий — как библиотекарь. Ты говоришь: "Дай мне книгу 'Гарри Поттер'", а библиотекарь сам решает, где её искать — на полке, в хранилище или заказать из другой библиотеки. Тебе не нужно знать деталей.

---

### 3. Слой сервисов (Service Layer)

**Для разработчика:**
Определяет варианты использования системы. Чётко разделяет оркестрацию (какие шаги выполнить) от бизнес-логики:

```python
# Сервисный слой — точка входа в систему
def allocate(orderid: str, sku: str, qty: int, uow: UnitOfWork):
    line = OrderLine(orderid, sku, qty)
    with uow:  # Unit of Work управляет транзакцией
        product = uow.products.get(sku=line.sku)
        if product is None:
            raise InvalidSku(f'Invalid sku {line.sku}')
        batchref = product.allocate(line)
        uow.commit()  # Явный коммит — безопасность по умолчанию
    return batchref

# Веб-контроллер становится тонким адаптером
@app.route("/allocate", methods=['POST'])
def allocate_endpoint():
    uow = SqlAlchemyUnitOfWork()
    batchref = services.allocate(
        request.json['orderid'],
        request.json['sku'],
        request.json['qty'],
        uow
    )
    return jsonify({'batchref': batchref}), 201
```

**Для школьника:**
Сервисный слой — как официант в ресторане. Ты говоришь: "Хочу стейк", официант передаёт заказ повару (доменная модель), следит, чтобы всё приготовили правильно, и приносит тебе готовое блюдо. Тебе не нужно знать, как именно повар жарил мясо.

---

### 4. Единица работы (Unit of Work)

**Для разработчика:**
Абстракция над атомарными операциями. Гарантирует согласованность данных:

```python
class AbstractUnitOfWork(abc.ABC):
    products: AbstractRepository
    
    def __enter__(self): 
        return self
    
    def __exit__(self, *args):
        self.rollback()
    
    @abc.abstractmethod
    def commit(self): ...
    @abc.abstractmethod
    def rollback(self): ...

# Использование через контекстный менеджер
def change_batch_quantity(ref: str, qty: int, uow: AbstractUnitOfWork):
    with uow:  # Всё или ничего
        batch = uow.products.get_by_batchref(ref)
        batch.change_quantity(qty)
        while batch.available_quantity < 0:
            line = batch.deallocate_one()
            # Автоматическая публикация события при коммите
        uow.commit()  # Явный коммит — система безопасна по умолчанию
```

**Для школьника:**
Представь, что ты перекладываешь конфеты из одной коробки в другую. Единица работы — это когда ты берёшь конфету из первой коробки И сразу кладёшь во вторую. Если уронишь конфету по дороге — всё действие отменяется, и конфеты остаются как были. Никаких "полу-переложенных" конфет!

---

### 5. Агрегаты и границы согласованности

**Для разработчика:**
Агрегат — кластер связанных объектов, которые мы рассматриваем как единое целое для обеспечения согласованности:

```python
class Product:  # Агрегат-корень
    def __init__(self, sku: str, batches: List[Batch]):
        self.sku = sku
        self.batches = batches  # Внутренние объекты
    
    def allocate(self, line: OrderLine) -> str:
        # Вся логика изменения находится здесь
        # Гарантируем инвариант: "нельзя выделить больше, чем есть в наличии"
        batch = next(b for b in sorted(self.batches) if b.can_allocate(line))
        batch.allocate(line)
        self.version_number += 1  # Для оптимистичной блокировки
        return batch.reference
```

Оптимистичная блокировка через `version_number` позволяет избежать блокировок всей таблицы при высокой нагрузке.

**Для школьника:**
Агрегат — как коробка с конструктором Лего. Внутри много деталей (батчи), но ты всегда работаешь со всей коробкой целиком. Если хочешь что-то изменить — берёшь всю коробку, меняешь детали внутри, и ставишь обратно. Никто другой не может трогать эту коробку, пока ты с ней работаешь.

---

## 🔷 Часть II: Масштабирование — Событийно-ориентированная архитектура

### 1. Доменные события (Domain Events)

**Для разработчика:**
События фиксируют факты, произошедшие в системе. Позволяют разделить основной сценарий и побочные эффекты:

```python
# Событие как объект данных
@dataclass
class OutOfStock(Event):
    sku: str

# Модель публикует события
class Product:
    def allocate(self, line: OrderLine) -> str:
        try:
            # ...логика выделения
        except StopIteration:
            self.events.append(OutOfStock(sku=line.sku))  # Не исключение!
            return None

# Шина сообщений маршрутизирует события к обработчикам
EVENT_HANDLERS = {
    OutOfStock: [send_out_of_stock_notification],
}

def handle(event: Event, uow: UnitOfWork):
    for handler in EVENT_HANDLERS[type(event)]:
        try:
            handler(event, uow=uow)  # Ошибки в обработчиках не ломают основную транзакцию
        except Exception:
            logger.exception('Ошибка обработки события')
```

**Для школьника:**
Когда заканчиваются конфеты в магазине, кассир не бежит сам звонить поставщику. Он просто ставит табличку "Закончились конфеты". А кладовщик, увидев табличку, сам решает, что делать — позвонить поставщику или предложить покупателю шоколад. Табличка — это событие.

---

### 2. Команды против событий

**Для разработчика:**
| Характеристика | Команда (Command) | Событие (Event) |
|----------------|-------------------|-----------------|
| Назначение | Инструкция системе сделать что-то | Факт о произошедшем |
| Именование | Повелительное наклонение (`Allocate`) | Прошедшее время (`Allocated`) |
| Обработка ошибок | Провал команды = ошибка системы | Провал обработки события = логируем и продолжаем |
| Отправка | Одному получателю | Всем заинтересованным |

```python
# Команда — ожидаем конкретного результата
class Allocate(Command):
    orderid: str
    sku: str
    qty: int

# Событие — информируем о факте
@dataclass
class Allocated(Event):
    orderid: str
    sku: str
    qty: int
    batchref: str
```

**Для школьника:**
Команда — как приказ "Принеси воду!". Если не принёс — задача не выполнена. Событие — как сообщение "Вода принесена". Если никто не услышал — вода всё равно принесена, просто никто не знает.

---

### 3. Интеграция через события (микросервисы)

**Для разработчика:**
Вместо синхронных вызовов между сервисами используем асинхронные события:

```python
# Внешний потребитель событий (например, из Redis)
def handle_change_batch_quantity(message):
    data = json.loads(message['data'])
    cmd = ChangeBatchQuantity(ref=data['batchref'], qty=data['qty'])
    messagebus.handle(cmd, uow=SqlAlchemyUnitOfWork())  # Внутренняя обработка

# Публикация исходящих событий
def publish_allocated_event(event: Allocated, _):
    redis.publish('line_allocated', json.dumps(asdict(event)))
```

Преимущества:
- Отказ одного сервиса не ломает всю систему (временная расцепка)
- Легко добавлять новые потребители событий без изменения источника
- Высокая масштабируемость — обработчики можно запускать параллельно

**Для школьника:**
Представь почтовый ящик на входе в дом. Курьер бросает письмо (событие) в ящик и уходит — ему не нужно ждать, пока кто-то прочитает письмо. А жильцы дома могут проверять ящик когда им удобно и реагировать на письма по своему усмотрению. Если один жилец заболел и не читает почту — остальные всё равно получают свои письма.

---

### 4. CQRS (Command Query Responsibility Segregation)

**Для разработчика:**
Разделение операций записи (команд) и чтения (запросов):

```python
# Запись через доменную модель
def allocate(cmd: Allocate, uow: UnitOfWork):
    with uow:
        product = uow.products.get(cmd.sku)
        product.allocate(OrderLine(cmd.orderid, cmd.sku, cmd.qty))
        uow.commit()  # Обновляем "записывающую" модель

# Чтение через оптимизированную модель
def allocations(orderid: str, uow: UnitOfWork):
    # Простой запрос без джойнов — миллисекунды вместо секунд
    return list(uow.session.execute(
        "SELECT sku, batchref FROM allocations_view WHERE orderid = :orderid",
        dict(orderid=orderid)
    ))
```

Обновление модели чтения через обработчик событий:
```python
def add_allocation_to_read_model(event: Allocated, uow: UnitOfWork):
    uow.session.execute(
        "INSERT INTO allocations_view VALUES (:orderid, :sku, :batchref)",
        dict(orderid=event.orderid, sku=event.sku, batchref=event.batchref)
    )
```

**Для школьника:**
В библиотеке есть два отдела:
1. **Отдел приёма книг** — строгий, проверяет каждую книгу, ставит штампы, заносит в главный журнал (запись через доменную модель)
2. **Читальный зал** — здесь лежат уже готовые каталоги и указатели, чтобы быстро найти книгу (оптимизированная модель чтения)

Когда приходит новая книга, сначала её обрабатывают в первом отделе, а потом обновляют каталоги во втором. Но читатели никогда не ходят в отдел приёма — они пользуются готовыми каталогами.

---

## 🚀 Как это применить как Team Lead

### 1. Начните с малого
- Внедрите **сервисный слой** даже в монолите — это даст мгновенный выигрыш в тестируемости
- Выделите **агрегаты** в сложных частях системы — упростит поддержку и снизит количество багов

### 2. Архитектурные решения под нагрузку
| Проблема | Решение из книги |
|----------|------------------|
| Медленные отчёты | CQRS + денормализованная модель чтения |
| Взаимоблокировки при высокой нагрузке | Агрегаты + оптимистичная блокировка |
| Сложная интеграция сервисов | Событийная архитектура + временна́я расцепка |
| Медленные тесты | Репозиторий + фейковые реализации |

### 3. Культура команды
- **Тестовая пирамида**: 70% юнит-тестов на сервисный слой, 20% интеграционных, 10% E2E
- **Явные зависимости**: избегайте импорта инфраструктуры в доменную модель
- **События как документация**: события в коде — это живая документация бизнес-процессов

### 4. Что НЕ делать
- Не применяйте все паттерны сразу — начните с 1-2 самых болезненных проблем
- Не стройте микросервисы до тех пор, пока не освоите агрегаты в монолите
- Не используйте события для всего подряд — простые операции не требуют событийной архитектуры

---

## 💡 Заключение: путь к должности Team Lead

Ключевая компетенция руководителя разработки — **умение принимать архитектурные решения, которые упрощают жизнь команды через 6-12 месяцев**. Книга "Architecture Patterns with Python" даёт вам:

1. **Язык для общения** с бизнесом через доменную модель (убиквитивный язык)
2. **Инструменты для управления сложностью** через чёткие границы ответственности
3. **Практические паттерны** для построения систем, которые масштабируются вместе с бизнесом

Начните применять эти идеи в текущих проектах — даже частично. Умение объяснить команде, **почему** мы разделяем чтение и запись, или **как** агрегаты помогают избежать взаимоблокировок — это то, что отличает технического лидера от старшего разработчика.

> "Архитектура — это решения, которые трудно изменить позже. Хорошая архитектура делает правильные вещи лёгкими, а неправильные — трудными." — адаптировано из книги