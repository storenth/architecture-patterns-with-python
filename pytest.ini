[pytest]
# Enable live logging to the terminal
log_cli = true

# Set the minimum log level to capture and display
log_cli_level = DEBUG

# Optional: Customize how the logs look in your terminal
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s (%(filename)s:%(lineno)s)
